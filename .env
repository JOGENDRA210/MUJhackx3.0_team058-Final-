REACT_APP_API_URL=http://localhost:5000

# Add your MongoDB connection string here (optional). If omitted, the server will use the
# default local MongoDB at mongodb://localhost:27017/skillsync
# Example (Atlas):
# MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.abcdz.mongodb.net/skillsync?retryWrites=true&w=majority"

# JWT secret for signing tokens (override in production)
# JWT_SECRET=replace-with-a-long-secret

# Use a local JSON file DB for development instead of MongoDB. Set to 'true' to enable.
USE_LOCAL_DB=true