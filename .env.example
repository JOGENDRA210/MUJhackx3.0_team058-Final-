# Rename this file to .env and fill in the values. Do NOT commit .env to version control.

# API URL used by the React app to talk to the Express server (optional override)
REACT_APP_API_URL=http://localhost:5000

# MongoDB connection string. If omitted the app will use the default local URI:
# mongodb://localhost:27017/skillsync
# Examples:
# - Local (no auth): mongodb://localhost:27017/skillsync
# - MongoDB Atlas (replace <username>, <password>, <cluster> and optional params):
#   MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/skillsync?retryWrites=true&w=majority"

MONGODB_URI=mongodb+srv://jogendra2112004yadav:4GLLW3YdW506wx61@cluster0.p9ukpzg.mongodb.net/

# JWT secret used to sign tokens (override in production). Keep this secret.
JWT_SECRET=replace-with-a-long-secret

NODE_ENV=development
PORT=5000
